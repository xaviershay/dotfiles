if RAILS_ENV == "test" && $stdin.tty?
  require 'redgreen'
end

class BigDecimal
  def inspect
    "BD: #{to_s}"
  end
end

class Money
  def inspect
    "M: #{to_s}"
  end
end

require 'hirb'

Hirb.enable :pager => false
