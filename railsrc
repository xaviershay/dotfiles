require 'redgreen' if RAILS_ENV == "test"

class BigDecimal
  def inspect
    "BD: #{to_s}"
  end
end

class ActiveRecord::Base
  def self.find_last
    find(:first, :order => 'created_at DESC')
  end
end
